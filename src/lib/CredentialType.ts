/**
 * The type of VeraId credential.
 */
export enum CredentialType {
  /**
   * The credential is an organisation signature bundle.
   */
  ORG_SIGNATURE_BUNDLE = 'application/vnd.veraid.signature-bundle',
}
